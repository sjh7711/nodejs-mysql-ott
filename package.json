{
  "name": "WHATFLECHA",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --watch db.json",
    "bulid": "CI= npm run build"
  },
  "devDependencies": {
    "json-server": "^0.16.3"
  }
}